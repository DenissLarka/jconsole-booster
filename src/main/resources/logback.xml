<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<!-- Console appender for development and immediate feedback -->
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- File appender for persistent logging -->
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${user.home}/.jconsole-booster/logs/jconsole-booster.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- Daily rollover -->
			<fileNamePattern>${user.home}/.jconsole-booster/logs/jconsole-booster.%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- Keep 7 days of history -->
			<maxHistory>7</maxHistory>
			<totalSizeCap>100MB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Async appender for better performance -->
	<appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
		<queueSize>512</queueSize>
		<discardingThreshold>0</discardingThreshold>
		<appender-ref ref="FILE" />
	</appender>

	<!-- Logger configurations -->

	<!-- Root logger - default level INFO, can be overridden with -debug flag -->
	<root level="INFO">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="ASYNC" />
	</root>

	<!-- JConsole Booster specific loggers -->
	<logger name="com.druvu.jconsole" level="INFO" additivity="false">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="ASYNC" />
	</logger>

	<!-- Debug mode for inspector components (can be noisy) -->
	<logger name="com.druvu.jconsole.inspector" level="INFO" />

	<!-- Third-party libraries - keep at WARN to reduce noise -->
	<logger name="org.glassfish" level="WARN" />
	<logger name="javax.management" level="WARN" />

</configuration>
